# Configs

Versioned configuration files for model selection, training and evaluation live here.

Configs should prefer explicit, reproducible values over hidden defaults. Model-specific configs belong under `configs/models/` once the initial foundation model is selected.
