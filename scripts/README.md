# Scripts

Utility scripts for dataset validation, preprocessing, evaluation, model conversion, reporting, and deployment helpers live here.

Scripts should avoid embedding secrets and should expose reproducible command-line arguments where practical.
